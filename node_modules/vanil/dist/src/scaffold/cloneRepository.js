"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cloneRepository = void 0;
const child_process_1 = require("child_process");
const cloneRepository = async (url) => {
    const cloneDir = '.clone';
    // TODO: use shelljs?
    (0, child_process_1.execSync)(`rm -rf ${cloneDir}`, {
        stdio: 'inherit',
    });
    (0, child_process_1.execSync)(`mkdir -p ${cloneDir}`, {
        stdio: 'inherit',
    });
    (0, child_process_1.execSync)(`git clone ${url} ${cloneDir}`, {
        stdio: 'inherit',
    });
    return cloneDir;
};
exports.cloneRepository = cloneRepository;
