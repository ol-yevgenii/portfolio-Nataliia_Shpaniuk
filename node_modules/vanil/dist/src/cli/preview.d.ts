/// <reference types="node" />
import { Config } from '../@types/config';
/** preview HTTP server that exactly mimics a static webserver */
export declare const preview: (config: Config, autoListen?: boolean) => Promise<{
    app: import("express-serve-static-core").Express;
    server: import("http").Server | import("https").Server;
}>;
export declare const printServerRunning: (tag: string, config: Config) => void;
