"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.build = void 0;
const orchestrate_1 = require("../core/orchestrate");
const clean_1 = require("./clean");
/**
 * cleans the dist folder and then builds in production mode,
 * therefore code optimizations are activated
 * (resulting code is not debuggable anymore)
 */
const build = async (config) => {
    // clean first
    await (0, clean_1.clean)(config);
    await (0, orchestrate_1.orchestrateTransformAll)({
        config,
        command: 'build',
        mode: 'production',
    });
    return 0;
};
exports.build = build;
