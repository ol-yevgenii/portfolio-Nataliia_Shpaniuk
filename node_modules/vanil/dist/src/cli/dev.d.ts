import { Config } from '../@types/config';
import * as WebSocket from 'ws';
import { Context } from '../@types/context';
export declare const triggerTransformAll: (serverConfig: any, config: Config, devWebSocketServer: WebSocket.Server) => Promise<Context>;
/**
 * dev HTTP server that watches for src folder changes and
 * informs connected webrowsers for when rebuilds of pages have happened
 */
export declare const dev: (config: Config) => Promise<void>;
/** inform all connected websocket clients about a change */
export declare const notifyChange: (devWebSocketServer: WebSocket.Server, context: Context) => void;
export declare const getLiveReloadUrl: (config: Config) => string;
