import { Context } from '../../../@types/context';
import { LoaderFn, LoaderRegisterFn } from '../interface';
/** transforms markdown to VDOM */
export declare const markdownToVdom: (md: string, context: Context) => any;
/** resolves a given target path to an absolute path on disk and returns its UTF8 contents */
export declare const mdVdomLoader: LoaderFn;
export declare const registerMdVdomLoader: LoaderRegisterFn;
