import { Context } from '../../@types/context';
/** writes out the generated content as a file in dist folder */
export declare const persistVanilPage: (context: Context, content: string) => Promise<void>;
/**
 * persists an arbitrary file, resolving its absolute destination path by
 * only having a path relative to the dist folder
 */
export declare const persistFileDist: (relativeTargetPath: string, content: string, context: Context) => Promise<void>;
/** persists a file and recursively creates the path if necessary */
export declare const persistFileAbsolute: (destPath: string, content: string) => void;
