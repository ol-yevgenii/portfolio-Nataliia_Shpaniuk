import { Context } from '../../@types/context';
import './tsx';
import './runtime';
export interface ScriptExecutionError {
    errorType: string;
    errorMessage: string;
    linesOfError?: Array<string>;
    original?: any;
}
export interface ExecutionResult<D, S = any> {
    data?: D;
    state?: S;
    error?: ScriptExecutionError;
}
export declare const run: <D, S>(scriptCode: string, context: Context) => Promise<ExecutionResult<D, S>>;
