"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.chunk = void 0;
/** recursively splits an array into chunks */
const chunk = (chunkSize, data, tail = []) => chunkSize === 0
    ? data // no chunks, return input
    : data.length === 0
        ? tail // not enough items left, return tail
        : // else slice into more pieces
            (0, exports.chunk)(chunkSize, data.slice(chunkSize), tail.concat([data.slice(0, chunkSize)]));
exports.chunk = chunk;
