import { Config } from '../../@types/config';
import { Mode } from '../../@types/context/mode';
export declare const getExecutionMode: () => Mode;
/** returns the default project config */
export declare const getDefaultConfig: () => Config;
export declare const finalizeConfig: (config: Config) => void;
