export interface LinkProps {
    href: string;
    rel?: string;
}
/** component for <likn> injection that will prevent double-injection */
export declare type LinkFn = (props: LinkProps) => VDOMNode;
