export interface ScriptProps {
    src: string;
    type?: string;
    children?: Array<string>;
}
/** component for <script> injection that will prevent double-injection */
export declare type ScriptFn = (props: ScriptProps) => VDOMNode;
