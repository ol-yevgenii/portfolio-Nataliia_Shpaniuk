export declare enum Commands {
    HELP = "help",
    VERSION = "version",
    DEV = "dev",
    BUILD = "build",
    PREVIEW = "preview",
    CHECK = "check",
    CLEAN = "clean",
    CONFIG = "config",
    INIT = "init"
}
export declare type Command = 'help' | 'version' | 'dev' | 'build' | 'preview' | 'check' | 'clean' | 'config' | 'init';
